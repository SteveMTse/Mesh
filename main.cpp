#include "Mesh.hpp"

int main(int argc, char* argv[]) {

    // Mesh m = Mesh(-1, 1, -1, 1, 100, 100);
    // m.set_node(0, 1, 1);
    // m.init_polygon(0, 3);
    // m.set_polygon(0, 1);
    // m.set_polygon(0, 2);
    // m.set_polygon(0, 3);


    Mesh m = Mesh("polyMesh.cm");
    return 0;
}